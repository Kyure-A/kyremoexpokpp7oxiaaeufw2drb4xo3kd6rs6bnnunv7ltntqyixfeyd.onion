server {
    listen       80;
    listen       [::]:80;
    server_name  _;

    location / {
        proxy_pass https://kyre.moe;
        proxy_set_header Host              kyre.moe;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
